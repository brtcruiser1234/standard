version: '3.8'

services:
  jazz-stats:
    build: .
    container_name: utah-jazz-stats
    ports:
      - "8888:3000"  # Map host port 8888 to container port 3000
    restart: unless-stopped
    labels:
      - "description=Utah Jazz Live Stats Dashboard with API Proxy"
